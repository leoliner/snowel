# Snowvel 小说生成器设计稿

> 基于雪花写作法的 AI 辅助小说创作系统

---

## 1. 产品概述

### 1.1 产品定位

Snowvel 是一款**AI 辅助小说创作工具**，基于《雪花写作法》的渐进式扩展理念，帮助作者从灵感火花系统化地构建完整长篇小说。系统强调**作者主导权**，AI 负责补足创作链条中最耗神的环节（灵感激发、设定管理、一致性守护），而非完全替代人类创作。

### 1.2 核心价值

- **解决卡文痛点**：在作者思路枯竭时提供灵感辅助
- **防遗忘机制**：通过结构化数据管理，防止"写着写着就忘了"
- **保持日更节奏**：预先生成场景卡片，降低每日创作决策负担
- **技巧显性化**：将隐性写作技巧（伏笔、误导等）转化为可追踪的数据资产

### 1.3 目标用户

- 网络连载小说作者（需要保持高频更新）
- 长篇虚构作品创作者（需要管理复杂设定）
- 新手作者（需要创作方法论指导）

---

## 2. 核心理念

### 2.1 雪花写作法（Snowflake Method）

由 Randy Ingermanson 提出的 10 步渐进式创作法，核心思想是**从小到大构建故事**：

1. 一句话梗概（≤25 词）
2. 一段式摘要（5 句：开端-3 冲突-结局）
3. 角色概要（每人一句故事线）
4. 一页纸大纲（扩展每句话为段落）
5. 角色详细档案（动机/目标/冲突/背景）
6. 四页纸扩展大纲（每段扩展为完整场景描述）
7. 角色全档案（完整人物小传）
8. 场景清单（每章的场景列表）
9. 场景卡片（每场景的 POV/目标/冲突/挫折）
10. 正文写作

### 2.2 技巧即资产

将写作技巧（伏笔、插叙、视角欺骗等）转化为**带生命周期的结构化数据**，而非仅依赖作者记忆：

```json
{
  "id": "foreshadow_001",
  "type": "伏笔",
  "content": "老宅阁楼铜镜有裂痕（实为封印裂缝）",
  "埋下章节": 12,
  "回收章节": 78,
  "关联人物": ["林妧"],
  "隐晦等级": "中",
  "状态": "已埋下|待回收",
  "回收提示": "镜中倒影首次异常时触发"
}
```

### 2.3 渐进式承诺

用户在每个阶段只需做最小决策，逐步深化：
- 先选标签（3 个关键词）
- 再挑梗概（3 选 1）
- 再确认摘要（5 句话）
- 最后才写正文

---

## 3. 系统架构

### 3.1 五层生成架构

```
┌─────────────────────────────────────────────────────────────┐
│                      Layer 5: 正文绽放                       │
│              单场景生成 + 技巧执行 + 一致性守护               │
└────────────────────────────┬────────────────────────────────┘
                             │ 场景卡片驱动
┌────────────────────────────▼────────────────────────────────┐
│                      Layer 4: 场景脉络                       │
│        四页纸扩展大纲 + 场景清单 + 场景卡片（含技巧标注）       │
└──────────────┬───────────────────────────────┬──────────────┘
               │                               │
   角色数据注入 │                               │ 情节框架注入
               │                               │
┌──────────────▼──────────────┐ ┌─────────────▼───────────────┐
│    Layer 3: 角色血肉        │ │    Layer 2: 故事骨架        │
│  角色档案库 + 角色关系图谱    │ │  一段式摘要 + 一页纸大纲      │
└─────────────────────────────┘ └─────────────┬───────────────┘
                                              │
                         ┌────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                      Layer 1: 灵感雪核                       │
│        一句话梗概 + 标签拼贴板 + 合规校验 + 钩子强化         │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 架构分层详解

#### Layer 1: 灵感雪核

**输入**：用户拖拽标签 / 自由输入关键词
**输出**：一句话梗概（≤25 词）+ 向 Layer 2 传递的标签要求包

**核心组件**：

1. **灵感拼贴板**
   - 题材基底：玄幻/赛博/古言/末世等
   - 核心冲突：替身文学/追妻火葬场/身份错位等
   - 感情线：暗恋成真/破镜重圆/救赎向等
   - 高概念钩子：自由输入区（如"如果道士用符咒黑进 AI 系统"）

2. **标签智能联动**
   - 题材选择后自动过滤冲突库（如"赛博修仙"隐藏"宫斗"标签）
   - 悬浮显示经典案例片段
   - 风格冲突预警（如"追妻火葬场+甜宠"提示不兼容）

3. **智能梗概工坊**
   - 模板引擎：根据标签组合动态调用模板
     ```
     模板1（身份冲突型）："当{主角}发现{真相}，他必须{行动}以{目标}，却{情感转折}"
     模板2（世界观驱动型）："在{世界观}中，{主角}为{目标}踏上{旅程}，却揭开{隐藏真相}"
     ```
   - AI 润色：注入情绪钩子/独特细节
   - 生成 3 版供用户选择

4. **合规校验**
   - 字数压缩：超 25 词时高亮冗余部分
   - 钩子检测：检查"主角+主动动词+独特冲突"三要素
   - 关键词覆盖度：标红未体现的标签

**向 Layer 2 传递的数据包**：
```json
{
  "core_hook": "道士用符咒黑进 AI 系统",
  "mandatory_elements": [
    {"tag": "赛博修仙", "requirement": "需体现科技与修仙的碰撞细节"},
    {"tag": "救赎向", "requirement": "主角需有从执念到释然的成长弧光"}
  ],
  "avoid_pitfalls": ["避免纯技术描写削弱修仙韵味"]
}
```

---

#### Layer 2: 故事骨架

**输入**：Layer 1 的梗概 + 标签要求包
**输出**：一段式摘要 + 一页纸大纲

**核心组件**：

1. **一段式摘要生成**
   - 严格 5 句结构：现状→冲突 1→冲突 2→冲突 3→结局
   - 自动嵌入 Layer 1 的 mandatory_elements
   - 每句话关联标签图标，点击可查看技巧建议

2. **一页纸大纲**
   - 扩展每句话为完整段落
   - 三幕剧标记：标出"激励事件""中点转折""高潮"
   - 进度比例检查：建置(25%)-对抗(50%)-结局(25%)

3. **可视化编辑**
   - 拖拽调整冲突顺序
   - 点击冲突点添加备注
   - 智能回溯提示：修改 Layer 2 时提示将影响 Layer 3-5

---

#### Layer 3: 角色血肉

**输入**：故事骨架（背景设定 + 关键事件）
**输出**：角色档案库 + 角色关系图谱

**核心组件**：

1. **角色画布**
   - 动机/目标/矛盾三角关系图
   - 道德灰度滑动条（纯良↔黑化）
   - 背景故事自动生成（关联大纲事件）

2. **角色档案结构**
   ```json
   {
     "name": "林风",
     "role": "主角",
     "motivation": "拯救苍生",
     "goal": "黑入天道 AI 修复系统",
     "conflict": "每次修复都在消耗自身记忆",
     "backstory": "千年前设计此 AI 的创造者转世",
     "arc": "从执念到自我救赎",
     "key_traits": ["符咒编程", "记忆缺失", "责任感"],
     "relationships": [
       {"target": "AI 系统", "type": "创造者-造物", "dynamic": "对抗"}
     ]
   }
   ```

3. **角色-情节联动**
   - 修改角色"核心恐惧"时自动扫描所有场景
   - 标出相关场景并建议强化描写
   - 预警：若最终决战场景与恐惧相关但无铺垫，提示需提前埋设

---

#### Layer 4: 场景脉络

**输入**：故事骨架 + 角色档案
**输出**：四页纸扩展大纲 + 场景清单 + 场景卡片

**核心组件**：

1. **四页纸扩展大纲**
   - 将一页纸大纲的每段扩展为详细场景描述
   - 自动注入角色状态变化
   - 技巧节点植入：在适当位置标注伏笔埋下/回收点

2. **场景清单**
   - 章节→场景映射表
   - 每章包含 3-6 个场景
   - 显示场景依赖关系（前置未完成时预警）

3. **场景卡片模板**
   ```json
   {
     "scene_id": "s_12_3",
     "chapter": 12,
     "scene_no": 3,
     "pov": "林妧",
     "goal": "取回铜镜",
     "conflict": "守卫阻拦 + 内心恐惧",
     "setback": "镜面裂痕扩大",
     "techniques": [
       {"type": "伏笔", "content": "裂痕泛青光", "foreshadow_id": "fs_001"},
       {"type": "视角欺骗", "content": "误判守卫意图"}
     ],
     "characters_present": ["林妧", "守卫"],
     "location": "老宅阁楼",
     "emotional_arc": "紧张→恐惧→决绝",
     "estimated_words": 2500
   }
   ```

4. **技巧地图可视化**
   - 时间轴上用图标标记所有技巧节点
   - 伏笔：埋下点(◐) / 回收点(◑)
   - 插叙：闪回符号(↺)
   - 视角欺骗：眼睛图标(👁)
   - 支持拖拽调整位置

---

#### Layer 5: 正文绽放

**输入**：场景卡片 + 角色状态 + 技巧库
**输出**：章节正文

**核心组件**：

1. **场景级上下文构建**
   - 检索相关角色当前状态
   - 拉取待回收伏笔列表
   - 提取前序场景关键信息

2. **技巧执行引擎**

   **伏笔执行**：
   - 生成埋下章节时创建伏笔资产
   - 到回收章节前 3 章系统预警
   - 回收时自动调取原文片段供参考

   **插叙执行**：
   - 根据大纲节点生成插叙内容
   - 自动添加过渡句（"望着雨幕，他忽然想起十年前那个雪夜…"）
   - 节奏检测：插叙长度≤本章 1/3

   **视角欺骗执行**：
   - 为"不可靠叙述者"章节添加约束：
     - 隐藏关键信息（"他没注意到袖口血迹"）
     - 添加双重解读描述（"她微笑点头"→实为强忍愤怒）
   - 反转校验：在真相揭示章节自动比对前期描述是否留有合理线索

3. **一致性守护**
   - **向量记忆库**：存储人物档案、关键事件、地点设定
   - **矛盾检测引擎**：
     - 规则引擎：检查人物年龄/地点逻辑
     - 小模型检测：识别矛盾描述（如"上文说雨停了，本章开头写暴雨"）
   - **自动更新**：生成后提取新增关键信息更新记忆库

4. **生成后处理**
   - 提取本场景新增伏笔并更新资产库
   - 标注矛盾高亮提示
   - 生成多版本对比供用户选择

---

## 4. 技巧策略库

### 4.1 技巧类型定义

| 技巧 | 定义 | 生命周期 | 防错设计 |
|------|------|----------|----------|
| **伏笔** | 前文埋设线索，后文回收呼应 | 埋下→强化记忆→回收→闭环 | 超期未回收预警；回收章节自动调取原文 |
| **插叙** | 中断主线插入过往片段 | 触发点→回忆内容→情绪锚点回归 | 长度≤本章 1/3；插叙后必有主线回归过渡 |
| **视角欺骗** | 利用有限视角误导读者 | 误导期→线索埋藏→真相揭示 | 反转时自动校验前期描述是否留有合理线索 |
| **草蛇灰线** | 若隐若现的线索链 | 多处轻触→逐渐清晰→最终串联 | 密度控制：每 10 章≤2 条新线索 |

### 4.2 技巧工作流示例（伏笔全生命周期）

```
大纲阶段
  ↓ 用户选定"铜镜伏笔"，系统在卷一大纲第 12 章标记【埋伏笔】，卷三第 78 章标记【回收】

写第 12 章
  ↓ 生成正文时 AI 加入"裂痕在月光下泛出异样青光"
  ↓ 自动创建伏笔资产（状态：已埋下）

写第 50 章
  ↓ 系统弹出提示："检测到第 12 章伏笔尚未回收，建议在本章增加镜中倒影异常细节（强化记忆）"

写第 78 章
  ↓ 系统高亮提示"回收伏笔：铜镜裂痕"
  ↓ 调取第 12 章原文片段供参考
  ↓ 生成后伏笔状态更新为"已回收"
  ↓ 技巧地图显示绿色闭环
```

### 4.3 技巧密度控制

- **克制原则**：默认设置技巧密度阈值（每 10 章≤2 个新伏笔）
- **自然度优先**：所有技巧需通过"是否符合人物动机/情境逻辑"校验
- **作者主权**：所有技巧建议均为"可关闭提示"，支持手动标记"此伏笔由我亲自回收"
- **新手友好**：提供"技巧强度滑块"（隐晦↔明显）

---

## 5. 核心数据模型

### 5.1 项目结构

```json
{
  "project_id": "uuid",
  "title": "项目标题",
  "created_at": "timestamp",
  "layer1": {
    "premise": "一句话梗概",
    "tags": ["赛博修仙", "替身文学", "救赎向"],
    "hook_score": 85
  },
  "layer2": {
    "paragraph_summary": "五句话摘要",
    "one_page_outline": "三幕剧大纲"
  },
  "layer3": {
    "characters": [{...}],
    "relationship_map": {...}
  },
  "layer4": {
    "four_page_outline": "扩展大纲",
    "scene_list": [{...}],
    "scene_cards": [{...}]
  },
  "layer5": {
    "chapters": [{...}]
  },
  "technique_hub": {
    "foreshadows": [{...}],
    "flashbacks": [{...}],
    "misdirections": [{...}]
  },
  "memory_vector": {
    "character_states": {...},
    "timeline": [...],
    "locations": {...}
  }
}
```

### 5.2 伏笔资产 Schema

```json
{
  "id": "fs_{chapter}_{index}",
  "type": "物品伏笔|情节伏笔|人物伏笔",
  "content": "描述",
  "埋设": {
    "chapter": 12,
    "scene": 3,
    "text": "原文片段",
    "subtlety": "高|中|低"
  },
  "回收": {
    "chapter": 78,
    "scene": 2,
    "status": "待回收|已回收",
    "trigger_condition": "镜中倒影异常时"
  },
  "linked_characters": ["char_id"],
  "related_foreshadows": ["fs_id"],
  "narrative_weight": "核心|重要|装饰"
}
```

### 5.3 场景卡片 Schema

```json
{
  "id": "scene_{chapter}_{no}",
  "chapter": 12,
  "scene_no": 3,
  "pov_character": "char_id",
  "scene_goal": "场景目标",
  "conflict": "冲突描述",
  "setback": "挫折/结果",
  "techniques": [
    {
      "type": "伏笔|插叙|视角欺骗",
      "params": {...}
    }
  ],
  "characters": ["char_id"],
  "location": "loc_id",
  "time": "相对时间",
  "emotional_arc": "情绪曲线",
  "prerequisites": ["scene_id"],
  "estimated_words": 2500,
  "status": "待生成|已生成|已修改"
}
```

---

## 6. 用户工作流程

### 6.1 完整创作流程

```
┌──────────────┐
│   新建项目    │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│ Layer 1: 灵感雪核                              │
│ 1. 从拼贴板拖拽 3 个标签                        │
│ 2. 查看实时生成的 3 版梗概                      │
│ 3. 选择/微调最终梗概（≤25 词）                  │
└────────────────────┬─────────────────────────┘
                     │ 点击"扩展骨架"
                     ▼
┌──────────────────────────────────────────────┐
│ Layer 2: 故事骨架                              │
│ 1. 查看生成的 5 句摘要（每句关联标签）          │
│ 2. 拖拽调整冲突顺序                            │
│ 3. 查看一页纸大纲（三幕剧标记）                │
│ 4. 确认或修改                                  │
└────────────────────┬─────────────────────────┘
                     │ 点击"塑造角色"
                     ▼
┌──────────────────────────────────────────────┐
│ Layer 3: 角色血肉                              │
│ 1. 查看 AI 生成的角色建议（基于骨架）          │
│ 2. 填写/修改角色档案                           │
│ 3. 查看关系图谱                                │
│ 4. 调节道德灰度等参数                          │
└────────────────────┬─────────────────────────┘
                     │ 点击"编织场景"
                     ▼
┌──────────────────────────────────────────────┐
│ Layer 4: 场景脉络                              │
│ 1. 查看四页纸扩展大纲                          │
│ 2. 浏览场景清单（章节→场景）                   │
│ 3. 在技巧地图上标注伏笔/插叙位置               │
│ 4. 编辑场景卡片（POV/目标/冲突/技巧）          │
│ 5. 拖拽调整场景顺序                            │
└────────────────────┬─────────────────────────┘
                     │ 点击"生成正文"
                     ▼
┌──────────────────────────────────────────────┐
│ Layer 5: 正文绽放                              │
│ 1. 选择要生成的场景                            │
│ 2. 预览场景卡片 + 关联角色状态                 │
│ 3. AI 生成正文（自动执行标注的技巧）           │
│ 4. 查看一致性检查结果                          │
│ 5. 对比多版本或修改                            │
│ 6. 确认后自动更新记忆库                        │
└──────────────────────────────────────────────┘
```

### 6.2 雪晶进度看板

**可视化雪晶图**：中心为一句话梗概，每完成一层向外扩展晶枝

**智能回溯机制**：
- 修改 Layer 1 时，高亮提示"将影响 Layer 2-5 共 XX 处内容"
- 提供选项：
  - 🔄 全量重生成（保留用户修改痕迹）
  - 📌 局部更新（仅重生成受影响章节）
  - 💡 差异对比（标红变更内容供确认）

---

## 7. 技术实现要点

### 7.1 系统组件

```python
class SnowvelNovelGenerator:
    def __init__(self):
        self.layers = {
            "layer1": InspirationCore(),      # 标签拼贴 + 梗概生成
            "layer2": StorySkeleton(),        # 摘要 + 大纲
            "layer3": CharacterForge(),       # 角色档案 + 关系图谱
            "layer4": SceneWeaver(),          # 场景卡片 + 技巧标注
            "layer5": TextBloom()             # 正文生成 + 一致性守护
        }
        self.technique_hub = TechniqueHub()   # 伏笔/插叙等策略库
        self.memory_vector = MemoryVector()   # 向量记忆库
        self.progress_board = SnowflakeBoard()  # 可视化看板
        self.compliance_checker = ComplianceChecker()  # 合规检查

    def expand_layer(self, current_layer, user_edits=None):
        """渐进式扩展：输入当前层内容，生成下一层"""
        if user_edits:
            self._smart_update(current_layer, user_edits)

        next_layer = self.layers[current_layer].generate(
            input=self.get_previous_layers(),
            technique_rules=self.technique_hub.get_active_rules()
        )
        self.progress_board.update(current_layer, next_layer)
        return next_layer

    def generate_scene(self, scene_card):
        """Layer 5: 基于场景卡片生成正文"""
        context = self._build_context(scene_card)
        text = self.layers["layer5"].write(
            prompt=scene_card.to_prompt(),
            constraints=context
        )

        # 提取新增伏笔并更新资产库
        new_foreshadows = self.technique_hub.extract_foreshadows(text, scene_card.chapter)
        self.technique_hub.add_foreshadows(new_foreshadows)

        # 一致性检查
        contradictions = self.compliance_checker.check(text, self.memory_vector)

        return {
            "text": text,
            "contradictions": contradictions,
            "new_foreshadows": new_foreshadows
        }
```

### 7.2 关键算法

**标签-模板匹配**：
```python
def match_template(selected_tags: List[Tag]) -> Template:
    """根据标签组合选择最优模板"""
    tag_types = [t.type for t in selected_tags]

    if "身份错位" in tag_types or "替身" in tag_types:
        return Template.IDENTITY_CONFLICT
    elif "赛博" in tag_types or "末世" in tag_types:
        return Template.WORLD_DRIVEN
    elif "救赎向" in tag_types:
        return Template.REDEMPTION
    else:
        return Template.GENERAL
```

**伏笔回收预警**：
```python
def check_foreshadow_alerts(current_chapter: int, foreshadows: List[Foreshadow]):
    """检查需要预警的伏笔"""
    alerts = []
    for fs in foreshadows:
        if fs.回收.status == "待回收":
            distance = fs.回收.chapter - current_chapter
            if distance <= 3 and distance > 0:
                alerts.append({
                    "type": "即将回收",
                    "foreshadow": fs,
                    "message": f"第 {fs.回收.chapter} 章有待回收伏笔"
                })
            elif current_chapter > fs.回收.chapter:
                alerts.append({
                    "type": "超期未回收",
                    "foreshadow": fs,
                    "message": f"伏笔已超期，建议立即回收或调整回收章节"
                })
    return alerts
```

### 7.3 AI 调用策略

| 任务 | 模型选择 | 优化策略 |
|------|----------|----------|
| 梗概生成 | 轻量级模型 | 快速生成 3 版候选 |
| 场景卡片生成 | 中等模型 | 结构化输出，强制 JSON |
| 正文生成 | 强模型 | 长上下文，创意写作 |
| 一致性检查 | 轻量模型 | 并行检查多个维度 |
| 向量嵌入 | Embedding 模型 | 批量处理，缓存优化 |

### 7.4 记忆库设计

**向量数据库结构**：
```
Collection: character_states
- 人物外貌特征
- 当前状态（伤势/情绪/持有物品）
- 关系动态

Collection: timeline
- 关键事件（时间/地点/参与者/结果）
- 信息揭示记录（读者已知 vs 角色已知）

Collection: locations
- 地点设定
- 在地点发生的关键事件
- 地点与人物的关系
```

---

## 8. 合规检查规则

### 8.1 Layer 1 检查

| 检查项 | 规则 | 修复建议 |
|--------|------|----------|
| 字数 | ≤25 词 | "删除'非常''突然'等副词" |
| 三要素 | 主角+主动动词+独特冲突 | "添加'当...时'句式强化冲突" |
| 钩子 | 含"如果...会怎样"结构 | 建议改写为高概念句式 |

### 8.2 Layer 2 检查

| 检查项 | 规则 | 修复建议 |
|--------|------|----------|
| 五句结构 | 现状→冲突 1→冲突 2→冲突 3→结局 | 标红缺失部分 |
| 情感代价 | 每个冲突需体现情感代价 | "第 3 句缺少情感代价描述" |
| 标签覆盖 | 所有 Layer 1 标签需有对应 | "未体现'救赎向'元素" |

### 8.3 Layer 4 检查

| 检查项 | 规则 | 修复建议 |
|--------|------|----------|
| 场景闭环 | 目标-冲突-挫折 | "本场景挫折感不足，建议增加..." |
| 节奏控制 | 每章 3-6 个场景 | 过多/过少预警 |
| 技巧密度 | 每 10 章≤2 个新伏笔 | "建议暂缓埋设新伏笔" |

---

## 9. 差异化亮点

1. **雪晶进度可视化** — 让用户感知创作成长，类似技能树的可视化反馈
2. **标签-模板引擎** — 降低启动门槛，拖拽即可生成专业梗概
3. **技巧全生命周期管理** — 从"写着写着就忘"到"系统化追踪"
4. **智能回溯机制** — 修改上层自动定位影响范围，避免全量重写
5. **角色-情节联动** — 修改人设自动扫描关联场景，防止"人设崩塌"
6. **合规检查即时反馈** — 将写作理论转化为可操作的规则提示

---

## 10. 实施路线图

### Phase 1: MVP（核心流程验证）
- Layer 1 → Layer 2 完整流程
- 基础标签库（10 个题材 × 10 个冲突 × 10 个感情线）
- 简单合规检查

### Phase 2: 增强版
- Layer 3 角色系统
- Layer 4 场景卡片
- 技巧地图（仅伏笔）

### Phase 3: 完整版
- Layer 5 正文生成
- 向量记忆库
- 一致性守护
- 完整技巧库（插叙、视角欺骗）

### Phase 4: 生态
- 社区标签库共享
- 插件市场（导出 EPUB/有声书适配）
- 协作功能

---

## 附录：参考理论

- **《雪花写作法》** (Randy Ingermanson) — 核心方法论
- **《故事》** (Robert McKee) — 情节力学
- **《作家之旅》** (Christopher Vogler) — 英雄之旅简化版
- **《金圣叹批评水浒传》** — 东方伏笔技法（草蛇灰线）
- **《故事形态学》** (Vladimir Propp) — 31 种叙事功能
- **认知叙事学** — 读者心理模拟与共情机制

---

*本文档基于雪花写作法理念，结合 AI 技术，旨在构建一个"懂叙事节奏的创作伙伴"。*
